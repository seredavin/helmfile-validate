run:
  timeout: 5m
  tests: true
  skip-dirs:
    - vendor
    - vendor-stubs

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - gofmt
    - goimports
    - misspell
    - gosec
    - gocritic
    - unconvert
    - prealloc
    - exportloopref
    - goconst
    - bodyclose
    - noctx
    - wastedassign
    - unparam
    - nakedret
    - errorlint
    - exhaustive
    - nolintlint
    - whitespace

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: true
  gocritic:
    enabled-tags:
      - diagnostic
      - experimental
      - opinionated
      - performance
      - style
    disabled-checks:
      - dupImport
      - ifElseChain
      - octalLiteral
      - whyNoLint
      - wrapperFunc
  goimports:
    local-prefixes: github.com/seredavin/helmfile-validate
  gosec:
    severity: medium
    confidence: medium
  govet:
    check-shadowing: true
  misspell:
    locale: US
  nolintlint:
    allow-leading-space: true
    allow-unused: false
    require-explanation: false
    require-specific: false

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - gosec
        - gocritic
        - goconst
    - path: vendor
      linters:
        - all
    - path: vendor-stubs
      linters:
        - all
  max-issues-per-linter: 0
  max-same-issues: 0
